import { DisableNagetiveDirective } from './disable-nagetive.directive';

describe('DisableNagetiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableNagetiveDirective();
    expect(directive).toBeTruthy();
  });
});
