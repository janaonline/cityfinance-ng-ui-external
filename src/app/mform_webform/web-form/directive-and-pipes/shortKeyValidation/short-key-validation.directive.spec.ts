import { ShortKeyValidationDirective } from './short-key-validation.directive';

describe('ShortKeyValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ShortKeyValidationDirective();
    expect(directive).toBeTruthy();
  });
});
