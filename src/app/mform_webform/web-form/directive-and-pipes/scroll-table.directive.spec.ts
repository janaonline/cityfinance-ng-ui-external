import { ScrollTableDirective } from './scroll-table.directive';

describe('ScrollTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollTableDirective();
    expect(directive).toBeTruthy();
  });
});
