import { ScrollTablePipe } from './scroll-table.pipe';

describe('ScrollTablePipe', () => {
  it('create an instance', () => {
    const pipe = new ScrollTablePipe();
    expect(pipe).toBeTruthy();
  });
});
