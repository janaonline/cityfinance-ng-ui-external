import { RestrictFirstDigitAsZeroDirective } from './restrict-first-digit-as-zero.directive';

describe('RestrictFirstDigitAsZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictFirstDigitAsZeroDirective();
    expect(directive).toBeTruthy();
  });
});
