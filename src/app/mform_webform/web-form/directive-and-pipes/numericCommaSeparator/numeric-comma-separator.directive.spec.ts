import { NumericCommaSeparatorDirective } from './numeric-comma-separator.directive';

describe('NumericCommaSeparatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericCommaSeparatorDirective();
    expect(directive).toBeTruthy();
  });
});
