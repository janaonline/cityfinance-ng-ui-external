import { NumberLengthDirective } from './number-length.directive';

describe('NumberLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberLengthDirective();
    expect(directive).toBeTruthy();
  });
});
