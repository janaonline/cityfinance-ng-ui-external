import { RestrictFirstCharAsSpaceDirective } from './restrict-first-char-as-space.directive';

describe('RestrictFirstCharAsSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictFirstCharAsSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
