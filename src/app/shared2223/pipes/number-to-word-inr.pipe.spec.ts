import { NumberToWordINRPipe } from './number-to-word-inr.pipe';

describe('NumberToWordINRPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToWordINRPipe();
    expect(pipe).toBeTruthy();
  });
});
